// Единые типы, чтобы не расходились между файлами

export type RoleItem = { label: string; value: number };     // для бара по ролям
export type LeagueBucket = { label: string; pct: number };    // для бара по лигам
